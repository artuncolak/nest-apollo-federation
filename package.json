{
  "private": true,
  "name": "ikas-backend-assignment",
  "author": "Artun Çolak <artuncolak97@gmail.com>",
  "scripts": {
    "build": "turbo run build",
    "dev:services": "turbo run start:dev --filter=!gateway",
    "dev:gateway": "turbo run start:dev --filter=gateway",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "eslint": "^8.40.0",
    "prettier": "^2.8.8",
    "turbo": "^1.9.4",
    "eslint-config-app": "*"
  }
}
